(()=>{
const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)],NS='http://www.w3.org/2000/svg';
const S=(at,type='methyl')=>({at,type});
const P=(name,n,subs=[],ring=false,aliases=[])=>({name,n,subs,ring,aliases});
const questions=[
P('ethane',2),P('propane',3),P('butane',4),P('pentane',5),P('hexane',6),P('2-methylpropane',3,[S(2)]),P('2-methylbutane',4,[S(2)]),P('2-methylpentane',5,[S(2)]),P('3-methylpentane',5,[S(3)]),P('2-methylhexane',6,[S(2)]),P('3-methylhexane',6,[S(3)]),P('cyclopropane',3,[],true),P('cyclobutane',4,[],true),P('cyclopentane',5,[],true),P('cyclohexane',6,[],true),P('methylcyclopropane',3,[S(1)],true),P('methylcyclobutane',4,[S(1)],true),P('methylcyclopentane',5,[S(1)],true),P('methylcyclohexane',6,[S(1)],true),P('ethylcyclohexane',6,[S(1,'ethyl')],true),
P('heptane',7),P('octane',8),P('2,2-dimethylpropane',3,[S(2),S(2)]),P('2,3-dimethylbutane',4,[S(2),S(3)]),P('2,2-dimethylpentane',5,[S(2),S(2)]),P('2,3-dimethylpentane',5,[S(2),S(3)]),P('2,4-dimethylpentane',5,[S(2),S(4)]),P('3,3-dimethylpentane',5,[S(3),S(3)]),P('3-ethylpentane',5,[S(3,'ethyl')]),P('3-ethylhexane',6,[S(3,'ethyl')]),P('4-ethylheptane',7,[S(4,'ethyl')]),P('propylcyclopentane',5,[S(1,'propyl')],true),P('propylcyclohexane',6,[S(1,'propyl')],true),P('ethylcyclopentane',5,[S(1,'ethyl')],true),P('1-ethyl-2-methylcyclopentane',5,[S(1,'ethyl'),S(2)],true),P('1-ethyl-3-methylcyclopentane',5,[S(1,'ethyl'),S(3)],true),P('1,2-dimethylcyclohexane',6,[S(1),S(2)],true),P('1,3-dimethylcyclohexane',6,[S(1),S(3)],true),P('1,4-dimethylcyclohexane',6,[S(1),S(4)],true),P('1-ethyl-3-methylcyclohexane',6,[S(1,'ethyl'),S(3)],true),
P('3-ethyl-2-methylhexane',6,[S(3,'ethyl'),S(2)]),P('3-ethyl-2,4-dimethylhexane',6,[S(3,'ethyl'),S(2),S(4)]),P('4-ethyl-2,2-dimethylheptane',7,[S(4,'ethyl'),S(2),S(2)]),P('4-ethyl-2,3-dimethylheptane',7,[S(4,'ethyl'),S(2),S(3)]),P('3-ethyl-2,2,4-trimethylhexane',6,[S(3,'ethyl'),S(2),S(2),S(4)]),P('4-ethyl-2,3,5-trimethylheptane',7,[S(4,'ethyl'),S(2),S(3),S(5)]),P('5-ethyl-2,2,4-trimethyloctane',8,[S(5,'ethyl'),S(2),S(2),S(4)]),P('2,2-dimethyl-5-propylnonane',9,[S(5,'propyl'),S(2),S(2)]),P('3,3-dimethyl-5-propylnonane',9,[S(5,'propyl'),S(3),S(3)]),P('2-methyl-4-(propan-2-yl)heptane',7,[S(4,'isopropyl'),S(2)],false,['2-methyl-4-isopropylheptane']),P('2,2-dimethyl-5-(propan-2-yl)nonane',9,[S(5,'isopropyl'),S(2),S(2)],false,['2,2-dimethyl-5-isopropylnonane']),P('5-tert-butyl-2,2-dimethylnonane',9,[S(5,'tertbutyl'),S(2),S(2)],false,['5-(tert-butyl)-2,2-dimethylnonane']),P('6-tert-butyl-3,3-dimethyldecane',10,[S(6,'tertbutyl'),S(3),S(3)],false,['6-(tert-butyl)-3,3-dimethyldecane']),P('4-cyclohexyl-2-methylheptane',7,[S(4,'cyclohexyl'),S(2)]),P('5-cyclopentyl-3-ethylnonane',9,[S(5,'cyclopentyl'),S(3,'ethyl')]),P('1-ethyl-2,4-dimethylcyclohexane',6,[S(1,'ethyl'),S(2),S(4)],true),P('1-ethyl-3,5-dimethylcyclohexane',6,[S(1,'ethyl'),S(3),S(5)],true),P('1-methyl-2-(propan-2-yl)cyclohexane',6,[S(1),S(2,'isopropyl')],true,['1-isopropyl-2-methylcyclohexane']),P('1-tert-butyl-3-ethylcyclohexane',6,[S(1,'tertbutyl'),S(3,'ethyl')],true,['1-(tert-butyl)-3-ethylcyclohexane']),P('1-tert-butyl-2,4-dimethylcyclohexane',6,[S(1,'tertbutyl'),S(2),S(4)],true,['1-(tert-butyl)-2,4-dimethylcyclohexane'])];
const shuffled=a=>{const x=[...a];for(let i=x.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[x[i],x[j]]=[x[j],x[i]]}return x};
const base={easy:questions.slice(0,20),moderate:questions.slice(20,40),hard:questions.slice(40)};
const orders={draw:{easy:shuffled(base.easy),moderate:shuffled(base.moderate),hard:shuffled(base.hard)},name:{easy:shuffled(base.easy),moderate:shuffled(base.moderate),hard:shuffled(base.hard)}};
let mode='draw',difficulty='easy',index=0,score=0,attempts=0,hintLevel=0,answered=false,nodes=[],edges=[],history=[],tool='draw',drag=null,tapStart=null,keyboardPoint={x:400,y:165};
const canvas=q('#alkane-board'),bondLayer=q('#drawn-bonds'),pointLayer=q('#drawn-points'),preview=q('#bond-preview'),tapMarker=q('#tap-marker'),keyboardCursor=q('#keyboard-cursor'),feedback=q('#name-feedback');
const active=()=>orders[mode][difficulty],el=(tag,attrs)=>{const e=document.createElementNS(NS,tag);Object.entries(attrs).forEach(([k,v])=>e.setAttribute(k,v));return e};
function molecularGraph(item,positions=false){
 const BOND=58,centerX=400,centerY=165,adj=Array.from({length:item.n},()=>[]),links=[],coords=[];const join=(a,b)=>{adj[a].push(b);adj[b].push(a);links.push([a,b])};
 if(item.ring){const radius=BOND/(2*Math.sin(Math.PI/item.n));for(let i=0;i<item.n;i++){const a=i*2*Math.PI/item.n;coords.push({x:centerX+radius*Math.cos(a),y:centerY+radius*Math.sin(a)})}for(let i=0;i<item.n;i++)join(i,(i+1)%item.n)}
 else{const dx=BOND*Math.cos(Math.PI/6),dy=BOND*Math.sin(Math.PI/6),start=centerX-dx*(item.n-1)/2;for(let i=0;i<item.n;i++)coords.push({x:start+i*dx,y:centerY+(i%2?dy/2:-dy/2)});for(let i=0;i<item.n-1;i++)join(i,i+1)}
 const used={};item.subs.forEach(sub=>{const p=sub.at-1,k=used[sub.at]||0;used[sub.at]=k+1;let angle;if(item.ring)angle=Math.atan2(coords[p].y-centerY,coords[p].x-centerX);else angle=k%2?((p%2)?-Math.PI/2:Math.PI/2):((p%2)?Math.PI/2:-Math.PI/2);const add=(parent,a)=>{const id=adj.length;adj.push([parent]);adj[parent].push(id);links.push([parent,id]);coords.push({x:coords[parent].x+Math.cos(a)*BOND,y:coords[parent].y+Math.sin(a)*BOND});return id};const c=add(p,angle);
  const turn=((sub.at+k)%2?1:-1)*Math.PI/3;
  if(sub.type==='ethyl')add(c,angle+turn);
  if(sub.type==='propyl'){const d=add(c,angle+turn);add(d,angle)}
  if(sub.type==='isopropyl'){add(c,angle-Math.PI/3);add(c,angle+Math.PI/3)}
  if(sub.type==='tertbutyl'){add(c,angle-Math.PI/3);add(c,angle);add(c,angle+Math.PI/3)}
  if(sub.type==='cyclopentyl'||sub.type==='cyclohexyl'){const count=sub.type==='cyclopentyl'?5:6,radius=BOND/(2*Math.sin(Math.PI/count)),center={x:coords[c].x+Math.cos(angle)*radius,y:coords[c].y+Math.sin(angle)*radius},ring=[c];for(let i=1;i<count;i++){const a=angle+Math.PI+i*2*Math.PI/count,id=adj.length;adj.push([]);coords.push({x:center.x+Math.cos(a)*radius,y:center.y+Math.sin(a)*radius});ring.push(id)}for(let i=0;i<count;i++)join(ring[i],ring[(i+1)%count])}
 });if(positions&&coords.length){const xs=coords.map(p=>p.x),ys=coords.map(p=>p.y),cx=(Math.min(...xs)+Math.max(...xs))/2,cy=(Math.min(...ys)+Math.max(...ys))/2;coords.forEach(p=>{p.x+=centerX-cx;p.y+=centerY-cy})}return{adj,links,coords};
}
function isomorphic(a,b){if(a.length!==b.length)return false;const ea=a.reduce((s,x)=>s+x.length,0),eb=b.reduce((s,x)=>s+x.length,0);if(ea!==eb)return false;const da=a.map(x=>x.length).sort().join(','),db=b.map(x=>x.length).sort().join(',');if(da!==db)return false;const order=[...a.keys()].sort((x,y)=>a[y].length-a[x].length),map=Array(a.length).fill(-1),used=Array(b.length).fill(false);function go(k){if(k===order.length)return true;const v=order[k];for(let w=0;w<b.length;w++){if(used[w]||a[v].length!==b[w].length)continue;let ok=true;for(let i=0;i<k;i++){const u=order[i],mu=map[u];if(mu>=0&&a[v].includes(u)!==b[w].includes(mu)){ok=false;break}}if(ok){map[v]=w;used[w]=true;if(go(k+1))return true;used[w]=false;map[v]=-1}}return false}return go(0)}
const svgPoint=e=>{const p=canvas.createSVGPoint();p.x=e.clientX;p.y=e.clientY;return p.matrixTransform(canvas.getScreenCTM().inverse())},nearest=(p,r=17)=>{let best=-1,d=r;nodes.forEach((n,i)=>{const x=Math.hypot(n.x-p.x,n.y-p.y);if(x<d){d=x;best=i}});return best},snap=(a,p)=>{const angle=Math.round(Math.atan2(p.y-a.y,p.x-a.x)/(Math.PI/6))*(Math.PI/6);return{x:a.x+64*Math.cos(angle),y:a.y+64*Math.sin(angle)}};
function save(){history.push(JSON.stringify({nodes,edges}));if(history.length>50)history.shift()}function compact(){const used=new Set(edges.flatMap(e=>[e.a,e.b])),map=new Map(),kept=[];nodes.forEach((n,i)=>{if(used.has(i)){map.set(i,kept.length);kept.push(n)}});edges=edges.map(e=>({a:map.get(e.a),b:map.get(e.b)}));nodes=kept}
function drawnGraph(){const a=nodes.map(()=>[]);edges.forEach(e=>{a[e.a].push(e.b);a[e.b].push(e.a)});return a}
const parentNames={2:'ethane',3:'propane',4:'butane',5:'pentane',6:'hexane',7:'heptane',8:'octane',9:'nonane',10:'decane'};
const substituentNames={methyl:'methyl',ethyl:'ethyl',propyl:'propyl',isopropyl:'propan-2-yl',tertbutyl:'tert-butyl',cyclopentyl:'cyclopentyl',cyclohexyl:'cyclohexyl'};
const parentName=item=>item.ring?'cyclo'+parentNames[item.n]:parentNames[item.n];
function groupSummary(item){if(!item.subs.length)return'no substituents';const counts={};item.subs.forEach(s=>counts[s.type]=(counts[s.type]||0)+1);return Object.entries(counts).map(([type,n])=>`${n===1?'one':n===2?'two':n===3?'three':n} ${substituentNames[type]} group${n===1?'':'s'}`).join(', ')}
function locationSummary(item){if(!item.subs.length)return'There are no substituents to number.';const grouped={};item.subs.forEach(s=>(grouped[s.type]||=[]).push(s.at));return Object.entries(grouped).map(([type,locants])=>`${substituentNames[type]} at ${locants.length===1?'carbon':'carbons'} ${locants.join(', ')}`).join('; ')+'.'}
function setNameFeedback(text,state=''){feedback.dataset.state=state;feedback.textContent=text}
function namingHint(){if(answered)return;const item=active()[index];hintLevel=Math.min(3,hintLevel+1);if(hintLevel===1){setNameFeedback(item.ring?`Hint 1/3: Begin with a ${item.n}-carbon ring as the parent.`:`Hint 1/3: Begin with the longest continuous chain of ${item.n} carbons.`);return}if(hintLevel===2){setNameFeedback(`Hint 2/3: Look for ${groupSummary(item)}.`);return}setNameFeedback(`Hint 3/3: ${locationSummary(item)} Number the parent for the lowest set of locants, then alphabetize different substituent names.`)}
function incorrectDrawing(target){if(!edges.length)return'Draw the complete carbon skeleton before checking your answer.';if(nodes.length!==target.adj.length)return`Your drawing contains ${nodes.length} carbon atom${nodes.length===1?'':'s'}; the named compound contains ${target.adj.length}.`;if(edges.length!==target.links.length)return`The carbon count is correct, but your drawing has ${edges.length} C–C bond${edges.length===1?'':'s'} instead of ${target.links.length}. Check for a missing or extra ring bond.`;const have=drawnGraph().map(x=>x.length).sort().join(','),need=target.adj.map(x=>x.length).sort().join(',');if(have!==need)return'The carbon count is correct, but the branching pattern is not. Check which parent carbons carry the substituents.';return'The numbers of carbons and bonds are correct, but one or more groups are connected at the wrong position.'}
function incorrectName(item,value){if(!value.trim())return'Enter a complete IUPAC name before checking your answer.';const entered=normalize(value),parent=normalize(parentName(item));if(!entered.includes(parent))return`Recheck the parent structure. Identify the ${item.ring?'ring':'longest continuous chain'} before naming its substituents.`;const expectedLocants=(item.name.match(/\d+/g)||[]).join(','),enteredLocants=(value.match(/\d+/g)||[]).join(',');if(expectedLocants!==enteredLocants)return'The parent name is correct, but the numbering is not. Number from the end or direction that gives the lowest set of locants.';return'The parent and locants look right. Recheck the substituent names, multiplicative prefixes, punctuation, and alphabetical order.'}
function renderCanvas(){bondLayer.replaceChildren();pointLayer.replaceChildren();edges.forEach((e,i)=>{const a=nodes[e.a],b=nodes[e.b];bondLayer.append(el('line',{x1:a.x,y1:a.y,x2:b.x,y2:b.y,class:'alkane-hit','data-edge':i}),el('line',{x1:a.x,y1:a.y,x2:b.x,y2:b.y,class:'alkane-bond','data-edge':i}))});nodes.forEach((n,i)=>pointLayer.append(el('circle',{cx:n.x,cy:n.y,r:9,class:'alkane-point','data-node':i})));tapMarker.replaceChildren();if(tapStart)tapMarker.append(el('circle',{cx:tapStart.x,cy:tapStart.y,r:11,class:'tap-origin'}));q('#draw-prompt').style.display=edges.length?'none':'block';q('#structure-count').textContent=`${nodes.length} carbon${nodes.length===1?'':'s'} · ${edges.length} bond${edges.length===1?'':'s'}`}
function structureSVG(item){const svg=q('#answer-structure'),g=molecularGraph(item,true);svg.replaceChildren();svg.setAttribute('aria-label',`Skeletal alkane structure for question ${index+1}. It contains ${g.coords.length} carbon atoms and ${g.links.length} carbon–carbon bonds.`);g.links.forEach(([a,b])=>svg.append(el('line',{x1:g.coords[a].x,y1:g.coords[a].y,x2:g.coords[b].x,y2:g.coords[b].y,class:'answer-bond'})))}
function fillMenu(){q('#name-question').replaceChildren();active().forEach((_,i)=>{const o=document.createElement('option');o.value=i;o.textContent=`Question ${i+1}`;q('#name-question').append(o)})}
function update(){const list=active(),item=list[index];hintLevel=0;answered=false;tapStart=null;q('#name-hint').disabled=false;q('#name-check').disabled=false;q('#name-family').textContent=difficulty[0].toUpperCase()+difficulty.slice(1);q('#name-prompt').textContent=mode==='draw'?`Draw ${item.name}.`:'Name this alkane.';q('#name-count').textContent=`Question ${index+1} of ${list.length}`;q('#name-question').value=index;q('#name-progress').style.width=`${(index+1)/list.length*100}%`;q('#name-score').textContent=`Score: ${score} / ${attempts}`;setNameFeedback(mode==='draw'?'Carbon atoms are implied at every line end and vertex.':'Enter the complete IUPAC name.');if(mode==='draw'){nodes=[];edges=[];history=[];renderCanvas()}else{q('#name-answer').value='';structureSVG(item)}}
function setMode(next){mode=next;index=0;q('#name-mode-draw').setAttribute('aria-pressed',next==='draw');q('#name-mode-name').setAttribute('aria-pressed',next==='name');q('#draw-mode-panel').hidden=next!=='draw';q('#name-mode-panel').hidden=next!=='name';fillMenu();update()}function setDifficulty(next){difficulty=next;index=0;['easy','moderate','hard'].forEach(x=>q(`#name-level-${x}`).setAttribute('aria-pressed',x===next));fillMenu();update()}function setTool(next){tool=next;tapStart=null;q('#draw-tool').classList.toggle('primary',next==='draw');q('#erase-tool').classList.toggle('primary',next==='erase');q('#draw-tool').setAttribute('aria-pressed',next==='draw');q('#erase-tool').setAttribute('aria-pressed',next==='erase');canvas.style.cursor=next==='draw'?'crosshair':'pointer';renderCanvas()}
function addBond(aPoint,bPoint){save();let a=nearest(aPoint);if(a<0){a=nodes.length;nodes.push({x:aPoint.x,y:aPoint.y})}let b=nearest(bPoint);if(b<0){b=nodes.length;nodes.push({x:bPoint.x,y:bPoint.y})}if(a!==b&&!edges.some(x=>(x.a===a&&x.b===b)||(x.a===b&&x.b===a)))edges.push({a,b});compact();tapStart=null;renderCanvas()}
function selectPoint(point){const near=nearest(point),chosen=near>=0?nodes[near]:{x:point.x,y:point.y};if(!tapStart){tapStart={x:chosen.x,y:chosen.y};renderCanvas();return}const end=snap(tapStart,chosen);addBond(tapStart,end)}
canvas.addEventListener('pointerdown',e=>{if(tool==='erase'){const hit=e.target.closest('[data-edge]');if(hit){save();edges.splice(+hit.dataset.edge,1);compact();renderCanvas()}return}const p=svgPoint(e),i=nearest(p),a=i>=0?nodes[i]:p;drag={i,a:{x:a.x,y:a.y},down:p,moved:false};preview.setAttribute('x1',a.x);preview.setAttribute('y1',a.y);preview.setAttribute('x2',a.x);preview.setAttribute('y2',a.y);canvas.setPointerCapture(e.pointerId)});canvas.addEventListener('pointermove',e=>{if(!drag)return;const raw=svgPoint(e);drag.moved=drag.moved||Math.hypot(raw.x-drag.down.x,raw.y-drag.down.y)>=12;if(!drag.moved)return;preview.hidden=false;const end=snap(drag.a,raw);preview.setAttribute('x2',end.x);preview.setAttribute('y2',end.y);const near=nearest(end);qa('.alkane-point').forEach((x,i)=>x.classList.toggle('near',i===near))});canvas.addEventListener('pointerup',e=>{if(!drag)return;const raw=svgPoint(e);if(drag.moved){tapStart=null;addBond(drag.a,snap(drag.a,raw))}else selectPoint(raw);preview.hidden=true;drag=null});canvas.addEventListener('pointercancel',()=>{preview.hidden=true;drag=null});
canvas.addEventListener('focus',()=>{keyboardCursor.hidden=false});canvas.addEventListener('blur',()=>{keyboardCursor.hidden=true});canvas.addEventListener('keydown',e=>{const step=e.shiftKey?32:64;if(['ArrowLeft','ArrowRight','ArrowUp','ArrowDown'].includes(e.key)){e.preventDefault();if(e.key==='ArrowLeft')keyboardPoint.x-=step;if(e.key==='ArrowRight')keyboardPoint.x+=step;if(e.key==='ArrowUp')keyboardPoint.y-=step*.86;if(e.key==='ArrowDown')keyboardPoint.y+=step*.86;keyboardPoint.x=Math.max(24,Math.min(776,keyboardPoint.x));keyboardPoint.y=Math.max(24,Math.min(306,keyboardPoint.y));keyboardCursor.setAttribute('cx',keyboardPoint.x);keyboardCursor.setAttribute('cy',keyboardPoint.y)}if(e.key==='Enter'||e.key===' '){e.preventDefault();selectPoint(keyboardPoint)}if(e.key==='Escape'){tapStart=null;renderCanvas()}});
q('#draw-tool').onclick=()=>setTool('draw');q('#erase-tool').onclick=()=>setTool('erase');q('#draw-undo').onclick=()=>{if(!history.length)return;const s=JSON.parse(history.pop());nodes=s.nodes;edges=s.edges;renderCanvas()};q('#draw-clear').onclick=()=>{if(edges.length){save();nodes=[];edges=[];renderCanvas()}};
const normalize=s=>s.toLowerCase().trim().replace(/[–—]/g,'-').replace(/\s/g,'');q('#name-check').onclick=()=>{if(answered)return;const item=active()[index],target=molecularGraph(item),value=q('#name-answer').value;let right;if(mode==='draw')right=isomorphic(drawnGraph(),target.adj);else right=[item.name,...item.aliases].some(x=>normalize(x)===normalize(value));attempts++;if(right){score++;answered=true;q('#name-hint').disabled=true;q('#name-check').disabled=true}q('#name-score').textContent=`Score: ${score} / ${attempts}`;setNameFeedback(right?'Correct.':`Incorrect — ${mode==='draw'?incorrectDrawing(target):incorrectName(item,value)}`,right?'correct':'incorrect')};q('#name-hint').onclick=namingHint;q('#name-previous').onclick=()=>{index=(index+active().length-1)%active().length;update()};q('#name-next').onclick=()=>{index=(index+1)%active().length;update()};q('#name-question').onchange=e=>{index=+e.target.value;update()};q('#name-mode-draw').onclick=()=>setMode('draw');q('#name-mode-name').onclick=()=>setMode('name');q('#name-level-easy').onclick=()=>setDifficulty('easy');q('#name-level-moderate').onclick=()=>setDifficulty('moderate');q('#name-level-hard').onclick=()=>setDifficulty('hard');q('#name-answer').addEventListener('keydown',e=>{if(e.key==='Enter')q('#name-check').click()});fillMenu();update();
})();
